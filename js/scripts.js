//keeping mind 'cat' becoming 'atc' and then 'atcay'
var userEntry = prompt("Enter word to translate:")

if (userEntry.charAt(0) == "a" || userEntry.charAt(0) == "e" || userEntry.charAt(0) == "i" || userEntry.charAt(0) == "o" || userEntry.charAt(0) == "u" ) {
  alert( userEntry + "ay")
  } else {

} 
var reorgBox = []
reorgBox.push(userEntry.split(""))
.chart(0)
alert(reorgBox)
